[package]
name = "forms_checker"
version = "0.1.0"
edition = "2021"

[dependencies]
shared_engine = { path = "../../shared/engine" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
